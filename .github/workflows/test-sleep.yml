name: Sleep Test Workflow

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  sleep-job:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep for 10 seconds
        run: sleep 10
